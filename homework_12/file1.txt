# 🚗 Car Rental Django Project (Dockerized)

This is a Django-based car rental application, fully containerized with Docker and Docker Compose. It uses PostgreSQL as the database, supports media file uploads (e.g., car photos), and includes automatic code reloading for development convenience.

---

## 📦 Features

- Django + PostgreSQL
- Persistent media storage (e.g., uploaded car images)
- Automatic server reload on code changes
- Data persists even after containers are removed (via Docker volumes)

---

## 🚀 Getting Started

### 1. docker-compose up -d

